class GuessingGame {
    constructor(min, max) {
        this.min = min;
        this.max = max;
        this.number = number;
    }

    set setRange(newMin, newMax) {
        this.min = newMin;
        this.max = newMax;
    }

    guess() {
        let result;
        if 
        else if (result === this.number) {
            return result;
        }

    }

    lower() {

    }

    greater() {

    }
}

module.exports = GuessingGame;
